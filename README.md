# 0xEdit

![License: MIT](https://img.shields.io/badge/license-MIT-blue.svg)
![Version](https://img.shields.io/badge/version-0.2.0-blue.svg)
![Platform](https://img.shields.io/badge/platform-macOS-important.svg)
[![CI](https://github.com/zhpeace/0xEdit/actions/workflows/ci.yml/badge.svg)](https://github.com/zhpeace/0xEdit/actions/workflows/ci.yml)

基于 **Tauri 2 + CodeMirror 6** 的跨平台桌面文本与十六进制编辑器。名字里的 `0x` 就是十六进制前缀——它既是产品最出彩的特色（十六进制编辑器），也是一枚烙在图标上的编辑器光标。

## ✨ 功能一览

| 类别 | 能力 |
|---|---|
| **核心编辑** | 多标签页 + 多标签平铺（「窗口」菜单：平铺标签上下/左右，SecureCRT Tile 风格自动网格排布，**文本文档与终端标签混合平铺**，平铺时顶部标签栏隐藏、格子标题条即标签（激活格高亮、未保存圆点、右键菜单、✕ 关闭）、每格独立编辑器与格内 Markdown 预览，合并标签一键收回单格，超过 12 个标签显示提示条）+ 文件树（Finder 式导航：单击箭头展开/收起、单击名称选中、双击进入目录或打开文件、路径栏面包屑逐级返回，本地/远程一致；文件显示大小列、目录不显示（避免符号链接/元数据误导）；隐藏文件默认过滤、眼睛开关一键显示、Office/字体/音频/视频等扩展名彩色图标；**目录树排序（Finder 式）**：文件夹恒在文件前、名称智能排序（数字感知/忽略大小写与标点），树行显示大小/类型/修改时间列（Finder 列表式，无数据显示留空），排序栏按「名称/大小/修改时间/类型」切换字段、再点同字段切换升/降序（偏好持久化，本地/远程/归档内统一；FTP 与归档无修改时间时该项回退名称排序），侧边栏可拖拽至窗口宽度 50%）、**目录树刷新**（右键菜单「刷新」、排序栏右上角刷新按钮、窗口重新获得焦点时自动重读当前目录——外部 Finder/终端改动后切回即见）、40+ 语言语法高亮（深色主题专用配色）、查找/替换（正则）、列编辑模式（Shift+方向键以键盘扩展矩形选区）、十六进制编辑器、编码检测/转换（UTF-8/16、GBK、Big5、Shift-JIS 等；自动检测优先 UTF-8，避免误判乱码）+ **字符集选择**（「文件 → 重新打开为…」或点击状态栏编码，按指定字符集重新解码查看，不改动磁盘内容）、Markdown 实时预览（编辑+预览双栏并存、滚动同步、工具栏按钮 ⇧⌘E）、8 套内置主题（含经典蓝、Solarized、Monokai 等）、**自动更新**（启动后静默检查 GitHub 最新版，有新版弹窗确认后下载并自动重启安装；帮助菜单「检查更新…」可手动触发） |
| **Markdown** | 格式工具栏（加粗/斜体/删除线/标题/引用/代码/链接/图片/列表/任务/表格/分割线）、格式快捷键（⌘B ⌘I ⌘K ⌘⇧X ⌘⇧C ⌘⇧A）、标题大纲（侧边栏）、粘贴/拖拽图片自动落盘到 assets/ 并插入链接、实时预览双栏 |
| **文本工具** | 代码折叠、书签（行号旁书签栏 + 书签面板 ⌘F2 添加，查找面板「全部加书签」批量标记匹配行）、行操作/排序、大小写转换、JSON/HTML/XML 格式化、Base64/URL 编解码、拆分窗口（VS Code 式左右分屏：可编辑 + 双向实时同步，⌘\ 开合，与 Markdown 预览互斥）、多光标 |
| **跨文件与远程** | 快速打开文件（⌘P，模糊匹配当前目录/最近文件/已打开标签）、跨目录查找替换、并排 diff 与合并、目录同步/比较、FTP/SFTP 远程文件树（服务器列表管理：MobaXterm Sessions 风格，未连接时显示服务器列表、行内连接/编辑/删除、双击连接、搜索过滤；连接后极简视图：顶部连接条 + 面包屑路径栏，操作全部收进右键——树空白处右键可刷新/上级/新建/上传/终端/断开，文件右键含打开/下载/复制路径/归档解压；远程面板双 tab（MobaXterm 风格）：「列表」管理服务器（搜索/新建/连接/复制/编辑/删除/双击连接，已连接行绿点标记）；「目录树」为唯一文件浏览视图，跟随当前激活的终端/会话——终端切到哪台服务器，目录树就显示哪台的文件树（各会话当前目录/展开/归档状态保留在内存，切回秒级恢复）；连接成功自动切到目录树并打开终端，断开会话（含删除服务器）联动关闭该服务器终端，断开最后一个自动回列表；私钥认证、加密传输、远程直接编辑回写、右键新建远程文件/文件夹、**右键「用默认应用打开」**（远程文件下载为本地临时副本后用系统默认应用打开，只读查看、改动不回传）、归档虚拟文件夹：本地/远程树中直接展开 zip/jar/war/ear/tar/tar.gz/tar.bz2/tar.xz/rar/7z/iso/cab/deb/rpm 等格式内部结构、zip 与 tar 家族条目以新 tab 打开编辑并回写归档、rar/7z 等只读浏览（保存时提示另存本地）、归档内条目右键菜单：打开/复制路径/刷新归档/提取到本地（文件或整目录解压，rar/7z 等外部格式同样支持）、归档文件右键菜单（WinRAR 风格：打开进入归档视图、解压文件…选目录、解压到当前文件夹/同名文件夹）、上传下载/重命名/删除、隐藏文件过滤联动（排序栏右上角刷新按钮与本地一致）、远程终端：连接后目录右键「在终端中打开」（或树空白右键），SSH PTY（xterm.js 渲染、自适应尺寸、多终端标签页（标签默认显示 用户名@账户名，悬停显示远端 shell 标题）、关闭即断开、⌘C/⌘V/⌘A 复制粘贴全选、终端内搜索 ⌘F、远端标题同步、跟随应用主题、⌘+/⌘- 字号调整、大输出背压防卡）；**传输进度 + 目录下载**：上传/下载/另存/归档缓存/远程保存回写统一为 64KB 分块传输，右下角浮条实时显示文件名+进度条+百分比（完成✓自动收起、失败红色提示），目录右键「下载到本地…」递归下载整个文件夹并重建本地层级（先统计文件数与总大小、逐文件分块传输、同一进度流、完成显示文件数））；本地文件树右键菜单（空白处：刷新、新建文件/文件夹、在终端打开；节点：打开、**用默认应用打开**（系统默认应用，如 PDF→预览、Word→WPS、目录则用文件管理器打开）、**打开所在文件夹**、**复制到…**/**移动到…**（弹目录选择器选目标文件夹，重名自动加序号不覆盖，目录递归复制，跨磁盘自动复制后删除）、新建文件/文件夹、重命名、删除、复制路径、在终端打开，归档文件额外支持解压文件…/解压到当前文件夹/解压到同名文件夹） |
| **自动化** | 宏录制/回放、JS 脚本、命令面板、Snippets、拼写检查、外部工具/终端集成 |
| **可靠性** | 自动保存、崩溃恢复、会话记忆（重启恢复全部标签页、回到上次光标位置并还原激活标签）、多窗口、打印/导出 PDF |

完整功能清单见 [FEATURES.md](./FEATURES.md)。

## 📸 截图预览

| 代码编辑 | 十六进制编辑器 |
|---|---|
| ![代码编辑](docs/screenshots/main-editor.png) | ![十六进制编辑器](docs/screenshots/hex-editor.png) |

| 设置面板（8 套主题） | 命令面板 |
|---|---|
| ![设置面板](docs/screenshots/settings-themes.png) | ![命令面板](docs/screenshots/command-palette.png) |

## 🛠 技术架构

| 层 | 技术 |
|---|---|
| 桌面壳 | Tauri 2（Rust 后端：FTP/SFTP、编码转换、目录遍历） |
| 前端 | Vanilla TypeScript + Vite（无框架，`src/app.ts` 为主控制器） |
| 编辑器引擎 | CodeMirror 6 |

```
src/            前端源码（模块化，按功能拆分）
src-tauri/      Rust 后端（lib.rs + Tauri 配置）
tests/          单元测试（Node）与 Playwright 冒烟测试
dist/           Vite 构建产物
```

## 环境要求

- Node.js ≥ 18
- Rust（stable）
- macOS 打包需要 Xcode Command Line Tools；跨平台打包需对应平台工具链

## 快速开始

```bash
# 安装依赖
npm install

# 开发模式（浏览器，仅前端）
npm run dev

# 桌面开发模式（Tauri 窗口，自动起前端）
npm run tauri dev

# 类型检查 + 构建前端产物
npm run build

# 打包桌面应用（.app / .dmg 等，产物在 src-tauri/target/release/bundle）
npm run tauri build
```

## 测试

```bash
# 全部测试：单元测试 + 端到端冒烟 + Rust 后端测试
npm test

# 仅单元测试（纯 Node，无需浏览器）
npm run test:unit

# 仅端到端冒烟（自动启动 dev server 并依次运行 5 个 Playwright 测试后关闭）
npm run test:e2e

# 仅 Rust 后端测试
npm run test:rust

# 单独运行某个冒烟测试（需先手动启动 dev server：npm run dev）
npm run test:i18n      # 三语界面
npm run test:merge     # 目录比较/合并
npm run test:markdown  # Markdown 预览/导出
npm run test:core      # 十六进制/宏/命令面板/编码转换
npm run test:verify    # 端到端功能验证
```

## 发布

```bash
npm run tauri build
```

macOS 产物输出于 `src-tauri/target/release/bundle/macos/*.app` 与 `dmg/*.dmg`。

## License

[MIT](./LICENSE)
