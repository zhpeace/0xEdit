# 0xEdit 功能清单

> 全部功能已实现并可用。新功能随开发同步补充。

## 核心编辑
- [x] 多标签页 + 文件树（Finder 式：箭头展开/收起、单击名称选中、双击进入目录或打开文件、面包屑逐级返回）
- [x] 目录树排序（Finder 式：文件夹恒前、名称智能排序；树行显示大小/类型/修改时间列；排序栏按名称/大小/修改时间/类型切换字段与升/降序，偏好持久化，本地/远程/归档内统一；侧边栏可拖拽至窗口 50%）
- [x] 新建 / 打开 / 保存 / 另存为（关闭前脏标记确认）
- [x] 语法高亮（40+ 语言自动识别，深色主题专用配色）
- [x] 查找/替换（正则、区分大小写、全词、高亮、全部替换、选中替换）
- [x] 列编辑模式（矩形选择、多行插列/删列、插入数字序列、列对齐、列复制）
- [x] 十六进制编辑器（虚拟滚动大文件、覆盖/插入模式、字节编辑/删除）
- [x] 编码检测/转换（UTF-8/BOM、UTF-16 LE/BE、GBK 互转、批量转码）
- [x] 代码折叠、跳转到行
- [x] 书签：行号旁书签栏（⌘F2 切换）、逐项跳转、书签面板（侧边栏列出全部书签，点击跳转）
- [x] 多光标（Alt+点击添加）
- [x] 自动补全词库（14+ 语言关键字与常用 API）

## 文本工具
- [x] 行操作：复制行、删除行、上下移动行、合并行、注释/取消注释
- [x] 大小写转换（大写/小写/首字母大写）、行尾转换（CRLF/LF/CR）
- [x] 行排序（按行/按列、升/降序、按数字/文本）、删除重复行/空行、去行尾空格
- [x] 制表符 ↔ 空格互转、显示空白字符/缩进线、自动换行开关
- [x] 字数统计（字符/单词/行/选区）
- [x] JSON/HTML/XML 格式化与压缩、Base64/URL 编解码
- [x] 拆分窗口（上下/左右同步编辑同一文件）
- [x] 多标签平铺（「窗口」菜单：平铺标签（上下/左右），SecureCRT Tile 风格自动网格排布、最多 12 格；平铺时顶部标签栏隐藏，格子标题条即标签：激活格高亮、未保存圆点、右键菜单（关闭/关闭其他/关闭所有/复制路径/在分屏中打开）、✕ 关闭；每格独立编辑器 + 格内 Markdown 预览（左右分栏，与单格一致），点击/键盘焦点激活跟随，关闭自动重排，剩 1 格自动合并，合并标签一键收回单格并恢复标签栏；平铺中再次点击平铺菜单仅切换方向、保留格子与预览状态；超过 12 个标签显示可点击提示条；hex/终端不参与平铺，点击 hex/终端标签自动合并）
- [x] 移除 Tauri 默认英文原生菜单（macOS 顶部系统菜单栏仅保留应用名），统一使用窗口内自绘中文菜单，跨平台行为一致
- [x] 自绘菜单开关项带 ✓ 勾选状态（侧边栏/Markdown 预览/自动换行/显示空白/标签换行/自动保存/平铺方向/当前语言）
- [x] 硬换行重排、插入日期时间、字符/ASCII 表、单词高亮
- [x] 拼写检查（英文词典 + 用户词典 + 忽略/替换）

## Markdown
- [x] 双栏实时预览（编辑+预览并存，⇧⌘E 切换，滚动同步、光标联动）
- [x] 格式工具栏（加粗/斜体/删除线/标题/引用/代码/链接/图片/列表/任务/表格/分割线）
- [x] 格式快捷键（⌘B ⌘I ⌘K ⌘⇧X ⌘⇧C ⌘⇧A）
- [x] 标题大纲（侧边栏，点击跳转）
- [x] 粘贴/拖拽图片自动落盘到 assets/ 并插入链接
- [x] 导出 HTML（可打印/存 PDF）、Word（.doc）、Markdown

## 文件树 / 归档
- [x] 本地文件树右键：打开、新建文件/文件夹、重命名、删除、复制路径、在终端打开
- [x] 隐藏文件过滤 + 眼睛总开关（本地/远程联动，状态持久化）
- [x] 扩展名彩色图标（Office/字体/音频/视频等）、本地文件大小列（目录留空）
- [x] 归档虚拟文件夹：zip/jar/war/ear/tar/tar.gz/tar.bz2/tar.xz/rar/7z/iso/cab/deb/rpm 等树内展开 + 进入视图
- [x] zip/tar 家族条目新 tab 编辑并回写归档；rar/7z 等只读浏览（保存时提示另存本地）
- [x] 归档内条目右键：打开、复制路径、刷新归档、提取到本地（文件/整目录）
- [x] 归档文件右键（WinRAR 风格）：打开进入视图、解压文件…（目录选择器）、解压到当前文件夹、解压到同名文件夹
- [x] 大文件流式查看/编辑（分页 + 行对齐分块）

## 跨文件 / 远程
- [x] 在文件中查找/替换（跨目录批量，结果停靠面板）
- [x] 文件比较（并排 diff 与合并显示）
- [x] 目录比较/同步（双目录 diff + 同步）
- [x] FTP/SFTP 远程文件树（私钥认证、加密传输、超时/断线处理、远程直接编辑回写）
- [x] 远程服务器列表管理（MobaXterm Sessions 风格）：未连接时面板显示服务器列表，行内 连接/编辑/删除 + 双击连接 + 搜索过滤；新建/编辑连接弹窗（协议/主机/端口/用户/密码可选保存/私钥/认证方式/起始目录/高级选项）；断开后自动回到列表
- 终端标签右键菜单：重命名会话 / 关闭 / 关闭其他 / 关闭所有 / 复制会话信息
- [x] 远程连接视图极简：顶部连接条（`SFTP · 主机:端口` + 终端 + 断开）+ 面包屑路径栏；操作全部收进右键——树空白处右键（刷新/上级/新建文件/新建文件夹/上传文件…/上传当前文件/在终端中打开/断开连接）、文件夹右键（新建/重命名/删除/在终端中打开等）、文件右键（打开/解压系列/下载到本地/重命名/复制路径/删除）
- [x] 远程面板双 tab（MobaXterm 风格）：「列表」= 服务器管理（搜索/新建/连接/复制/编辑/删除/双击连接，已连接行绿点标记）；「目录树」= 唯一文件浏览视图，跟随当前激活的终端/会话（终端标签切换即联动切换文件树；各会话当前目录/展开/归档状态保留在内存、切回秒级恢复）；连接成功自动切到目录树并打开终端；断开会话（含删除服务器）联动关闭该服务器的全部终端，关闭某服务器最后一个终端也反向断开会话（多终端时关一个不影响）；断开最后一个会话自动回列表；列表点已连接的服务器直接切换目录树到它
- [x] 远程右键：打开、新建文件/文件夹、重命名、删除、下载到本地、复制路径、归档解压到下载目录
- [x] 快速打开文件（⌘P，模糊匹配当前目录/最近文件/已打开标签）
- [x] 远程终端：连接后工具栏「终端」按钮 / 目录右键「在终端中打开」（cd 到目录）；SSH PTY 会话，xterm.js 渲染，自适应尺寸（容器变化同步远端列数/行数），多终端以标签页并行，关闭标签即断开并清理；⌘C/⌘V/⌘A 复制粘贴全选（无选区时 ⌘C 正常发中断）、右键粘贴、终端内搜索（⌘F 高亮/上下跳转）、远端标题同步到标签页、配色跟随应用主题、⌘+/⌘- 调整字号、输出背压防大文件卡顿
- [x] 传输进度（A 方案）：上传/下载/另存/归档缓存/远程保存回写统一 64KB 分块传输；右下角浮条实时进度（文件名 + 进度条 + 百分比），完成自动收起（✓），失败红条显示错误；FTP/SFTP 均支持
- [x] 文件夹下载（B 方案）：目录右键「下载到本地…」→ 选保存目录 → Rust 递归下载到「所选目录/远程文件夹名/」并重建完整层级（先统计文件数与总字节、逐文件分块下载、同一进度流、完成浮条显示文件数）

## 自动化
- [x] 宏录制/回放（保存/加载/管理）
- [x] JS 脚本（脚本编辑器驱动编辑器）
- [x] 命令面板（⇧⌘P）、Snippets（触发词 + Tab 跳转 + 管理器）
- [x] 外部工具 / 终端集成

## 界面 / 可靠性
- [x] 8 套主题（默认深/浅、经典蓝、Solarized、Monokai、高对比度、GitHub 浅色等），主题导出/导入
- [x] 设置面板（字号、主题、快捷键自定义、自动换行/空白/预览偏好）
- [x] 会话记忆：重启恢复标签页 + 回到上次光标位置
- [x] 自动保存、崩溃恢复、未保存内容恢复（.bak 备份 + 恢复对话框）
- [x] 剪贴板历史/多剪贴板、跳转位置历史（上/下一个编辑位置）
- [x] 标签分组/标签列表、标签拖拽排序、标签换行模式
- [x] 侧边栏拖拽调宽、多窗口、打印/导出 PDF、拖拽文件到窗口打开
- [x] 右键菜单 Esc 关闭、归档解压/提取使用系统目录选择器
